package io.minhasaude.msapi.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import io.minhasaude.msapi.model.DoencaComum;

public interface DoencaComumRepository extends JpaRepository<DoencaComum, Long>{
}
